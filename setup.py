#!/usr/bin/env python
raise SystemExit(
    "pyflann_ibeis now builds with scikit-build-core via pyproject.toml.\n"
    "Use one of:\n"
    "  uv pip install -e . --no-build-isolation\n"
    "  python -m build\n"
)
